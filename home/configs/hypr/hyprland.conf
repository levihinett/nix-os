# ~/.config/hypr/hyprland/hyprland.conf

# Mod key and terminal
$mod=SUPER
$terminal=kitty

# Animations
animations {
    bezier=smooth,0.05,0.9,0.1,1.0
    animation=windows,1,7,smooth
    animation=windowsOut,1,7,default,popin 80%
    animation=border,1,10,default
    animation=fade,1,7,default
    animation=workspaces,1,6,default
    enabled=true
}

# Keybindings
bind=$mod, RETURN, exec, $terminal
bind=$mod, Q, killactive
bind=$mod, L, exec, hyprlock
bind=$mod, M, exit
bind=$mod, D, exec, rofi -show drun
bind=$mod, E, exec, $terminal -e yazi
bind=$mod, F, fullscreen
bind=$mod, V, exec, cliphist list | rofi -dmenu | cliphist decode | wl-copy

# Focus movement
bind=$mod, left, movefocus, l
bind=$mod, right, movefocus, r
bind=$mod, up, movefocus, u
bind=$mod, down, movefocus, d

# Move windows
bind=$mod SHIFT, left, movewindow, l
bind=$mod SHIFT, right, movewindow, r
bind=$mod SHIFT, up, movewindow, u
bind=$mod SHIFT, down, movewindow, d

# Workspace switching
bind=$mod, 1, workspace, 1
bind=$mod, 2, workspace, 2
bind=$mod, 3, workspace, 3
bind=$mod, 4, workspace, 4
bind=$mod, 5, workspace, 5
bind=$mod, 6, workspace, 6
bind=$mod, 7, workspace, 7
bind=$mod, 8, workspace, 8
bind=$mod, 9, workspace, 9

# Move windows to workspace
bind=$mod SHIFT, 1, movetoworkspace, 1
bind=$mod SHIFT, 2, movetoworkspace, 2
bind=$mod SHIFT, 3, movetoworkspace, 3
bind=$mod SHIFT, 4, movetoworkspace, 4
bind=$mod SHIFT, 5, movetoworkspace, 5
bind=$mod SHIFT, 6, movetoworkspace, 6
bind=$mod SHIFT, 7, movetoworkspace, 7
bind=$mod SHIFT, 8, movetoworkspace, 8
bind=$mod SHIFT, 9, movetoworkspace, 9

# Decoration
decoration {
    blur {
        enabled=true
        passes=2
        size=8
    }
    shadow {
        enabled=true
        range=20
        render_power=3
    }
    rounding=12
}

# Startup applications
exec-once=waybar
exec-once=mako
exec-once=mkdir -p /home/levih/.cache/awww && awww-daemon
exec-once=sleep 1 && awww img -o eDP-1 /home/levih/Pictures/wallpapers/wallpaper.jpg

# General settings
general {
    border_size=2
    col.active_border=rgba(cba6f7ff)
    col.inactive_border=rgba(313244aa)
    gaps_in=6
    gaps_out=14
}

# Input
input {
    kb_layout=gb
}

# Monitor
monitor=,preferred,auto,1